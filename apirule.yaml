apiVersion: gateway.kyma-project.io/v1alpha1
kind: APIRule
metadata:
  name: ml-model
spec:
  gateway: kyma-gateway.kyma-system.svc.cluster.local
  service:
    name: ml-model
    port: 80
  rules:
  - path: /.*
    methods: ["GET","POST"]
    accessStrategies:
    - handler: allow
